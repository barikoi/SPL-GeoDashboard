# production
version: '3.7'
services:
  spl_geodashboard:
    container_name: spl-geodashboard
    image: rilusmahmud/spl-geodashboard:main-latest
    restart: unless-stopped
    environment:
      - NEXT_PUBLIC_BASE_URL=""
      - NEXT_PUBLIC_AUTOCOMPLETE_API_KEY=""
    ports:
      - "8071:3000"